<?xml version="1.0" encoding="utf-8"?>
<!-- Pantalla con la lista de docentes destacados de la UNTELS -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/fondo_principal">

    <!-- Encabezado de la pantalla -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/pantalla_padding">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/lista_profesores_titulo"
            android:textColor="@color/texto_acento"
            android:textSize="@dimen/texto_titulo"
            android:textStyle="bold" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/espacio_xs"
            android:text="@string/lista_profesores_subtitulo"
            android:textColor="@color/texto_claro"
            android:textSize="@dimen/texto_pequeno" />
    </LinearLayout>

    <!-- ListView con los docentes -->
    <ListView
        android:id="@+id/list_profesores"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:divider="@color/fondo_principal"
        android:dividerHeight="@dimen/espacio_xs" />

</LinearLayout>