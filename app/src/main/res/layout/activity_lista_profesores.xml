<!-- Layout principal para mostrar el listado de docentes con barra de búsqueda -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background_light">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="@dimen/padding_standard"
        android:text="@string/title_professors"
        android:textColor="@color/text_primary"
        android:textSize="24sp"
        android:textStyle="bold" />

    <EditText
        android:id="@+id/etSearchProfessor"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="@dimen/margin_standard"
        android:layout_marginBottom="@dimen/margin_small"
        android:hint="@string/hint_search_professor"
        android:inputType="text"
        android:padding="@dimen/margin_small"
        android:background="@drawable/ic_launcher_background"
        android:alpha="0.1" /> <!-- Nota: Deberíamos usar un background real en drawable -->

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvProfessors"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingHorizontal="@dimen/margin_small" />

</LinearLayout>
